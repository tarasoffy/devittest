// const { calculateTotalPrice, hasSufficientBalance, hasSufficientStock } = require("../../src/services/orderService");

// test("calculateTotalPrice should multiply price by quantity", () => {
//   expect(calculateTotalPrice(100, 2)).toBe(200);
// });

// test("hasSufficientBalance should return true if balance is enough", () => {
//   expect(hasSufficientBalance(500, 200)).toBe(true);
// });

// test("hasSufficientStock should return false if stock is insufficient", () => {
//   expect(hasSufficientStock(5, 10)).toBe(false);
// });
